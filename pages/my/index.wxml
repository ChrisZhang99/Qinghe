<!--index.wxml
<view class="container">
  <view  bindtap="bindViewTap" class="userinfo">
    <image class="userinfo-avatar" src="{{userInfo.baseInfo.avatarUrl}}" background-size="cover"></image>
    <text class="userinfo-nickname">{{userInfo.baseInfo.nickName}}</text>
  </view>
  <view class="info_list">
    <block wx:for="{{userListInfo}}">
        <view class="weui_cell">
            <view class="weui_cell_hd"><image src="{{item.icon}}"></image></view>
            <view class="weui_cell_bd">
                <view class="weui_cell_bd_p"> {{item.text}} </view>
            </view>
            
            <view wx:if="{{item.isunread}}" class="badge">{{item.unreadNum}}</view>
            <view class="with_arrow"></view>
        </view>
    </block>
  </view>
</view>
-->
<view class="container">
  <view class="nav_left">
    <block wx:for="{{leftMenus}}">
      <view class="weui_cell {{curMenuID == item.id ? 'active' : ''}}" bindtap="switchRightTab" data-index="{{index}}" data-id="{{item.id}}">
            <view class="weui_cell_hd"><image src="{{item.icon}}"></image></view>
            <view class="weui_cell_bd">
                <view class="weui_cell_bd_p"> {{item.text}} </view>
            </view>
      </view>
    </block>
  </view>
  <view class="nav_right">
    <view wx:if="{{navRightItems}}">
      <block wx:for="{{navRightItems}}">
        <view class="nav_right_items" data-index="{{index}}">
          <view class="nav_right_items_inside">
            {{item.CreatedTime}}
          </view>
          
          <block wx:for="{{item.PurchaseOrderItems}}">  
            <view class="nav_right_items_inside">      
                <view class='item_inside_product'>
                  {{item.Product.Name}}
                </view>

                <view class='item_inside_product'>
                  {{item.Count}}
                </view>
                <view class='item_inside_product'>
                  {{item.Product.Unit}}
                </view>
            </view>
          </block>
          
        </view>
      </block>
    </view>
    <view wx:else>暂无数据</view>
  </view>
</view>